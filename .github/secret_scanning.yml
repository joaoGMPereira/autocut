paths-ignore:
  # App-level OAuth credentials seeded into local SQLite DB on first install.
  # These are personal channel credentials, not server secrets.
  - "server/internal/database/migrations.go"
  - "server/internal/seed/assets/client_secret_*.json"
